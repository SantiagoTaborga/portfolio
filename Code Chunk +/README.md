# Code Chunk
This R code performs a regression analysis on a dataset to estimate the effects of mask usage and vaccination on the number of deaths, while controlling for population size and the social vulnerability index. It also generates a regression summary table using the `modelsummary` package, and formats it with the `gt` package for a clean presentation.
